- [x] 已形成统一的“当前已实现范围”清单，且与项目计划、进度台账、既有规格状态一致
- [x] 已形成完整的“剩余未完成功能”清单，覆盖 `ETL/CDM`、报表设计器、开放 API、系统/安全、测试交付五大域
- [x] 每个剩余功能域均已补足技术实现方案，包含模块职责、关键数据流、依赖边界与集成位置
- [x] 每个剩余功能域均已补足接口定义，至少明确路径或入口、核心字段、请求约束、响应结构与错误处理口径
- [x] 每个剩余功能域均已补足前后端交互逻辑说明，能够支撑页面、接口与后台行为的一致实现
- [x] `ETL/CDM` 剩余能力已明确新增主题顺序、`ODS/CDM` 映射、失败重试、并发控制、能力矩阵与下游复用要求
- [x] 报表设计器剩余能力已明确拖拽布局、联动配置、模板版本、发布/回滚及预览/导出/调度一致性要求
- [x] 开放 API 剩余能力已明确主题扩展、筛选规则、分页排序、scope 粒度、脱敏策略、日志与限流要求
- [x] 系统管理与安全剩余能力已明确字典治理、系统参数分类、细粒度权限、数据权限与生产安全基线要求
- [x] 字典治理已明确字典类型/字典项两级模型、编码唯一、启停、排序、软删除、内置项保护、引用校验与替代迁移规则
- [x] 系统参数治理已明确 `security/runtime/integration/feature/business` 分类、热更新边界、`prod` 可编辑性与敏感参数不得落库的托管规则
- [x] 权限模型已明确菜单权限、动作权限、数据范围权限、字段权限的固定判定顺序，以及前端降级显示与后端 `403/空集合/脱敏` 的一致行为
- [x] 数据范围与字段权限已明确“隐藏优先于脱敏、脱敏优先于明文”“多角色取最严”及导出/开放 API/审计快照复用同一裁剪逻辑
- [x] 生产安全基线已明确 `JWT/AES` active/previous key 轮换窗口、`HTTPS + HSTS`、Secret 挂载来源、启动拒绝条件与审计上下文字段要求
- [x] 所有剩余功能域都已补充异常处理机制，至少覆盖参数非法、权限不足、依赖失败、状态冲突与幂等性要求
- [x] 所有剩余功能域都已补充边界条件覆盖要求，至少覆盖空数据、超范围分页、重复提交、历史版本兼容与未配置依赖
- [x] 系统管理与安全模块已补充重复编码、引用中删除、内置项修改、并发更新冲突、敏感参数写入、越权访问与重复轮换计划的异常处理口径
- [x] 系统管理与安全模块已补充停用后禁止新增引用但保留历史回显、密钥兼容窗口结束后失效、证书/Secret 缺失即启动失败等边界条件要求
- [x] 已建立“功能域 -> 前端交互测试 -> 后端模块测试 -> `compose-smoke` -> 人工验收”分层映射，且不适用项需显式标注原因
- [x] 已定义覆盖率门禁提升方向、失败制品目录、统一回归入口与交付文档同步责任，可支撑后续实现与验收逐项执行
- [x] 已生成面向后续实现与验收的完整功能验证 checklist，可逐项勾选并追溯到对应模块
- [x] 本次新增规格与既有 `align-mvp-delivery`、`complete-mvp-hardening`、`advance-planned-phases` 三组规格之间不存在状态冲突
- [x] `/spec` 路径下已能清晰区分“历史已完成收口规格”和“当前剩余能力补全规格”
- [x] 本轮规格产物仅包含 `spec.md`、`tasks.md`、`checklist.md`，符合规格阶段约束
